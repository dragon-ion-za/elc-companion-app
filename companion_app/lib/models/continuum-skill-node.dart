import 'package:flutter/material.dart';

class ContinuumSkillNode {
  final String name;
  final Offset position;
  final int progression;

  ContinuumSkillNode(this.name, this.position, this.progression);
}